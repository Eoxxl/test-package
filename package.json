{
  "name": "hello-lib",
  "version": "0.1.0",
  "author": "",
  "license": "ISC",
  "description": "Test package",
  "main": "build/hello.js",
  "module": "build/hello.mjs",
  "types": "./build/hello.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "import": {
        "types": "./build/hello.d.ts",
        "default": "./build/hello.mjs"
      },
      "require": {
        "types": "./build/hello.d.ts",
        "default": "./build/hello.js"
      }
    }
  },
  "scripts": {
    "gen:mod": "node build_utils/create_mod.js",
    "build": "npm run clean && npm run gen:mod && tsup",
    "clean": "rm -rf build && rm -f ./mod.ts",
    "prepack": "npm run build",
    "release": "changeset version && git add . && git commit -m 'release: version bump' && git push",
    "changeset": "changeset"
  },
  "files": [
    "build"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@types/node": "^24.0.13",
    "glob": "^11.0.3",
    "ts-node": "^10.9.2",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
